#include <iostream>
#include <vector>
#include <cstdio>
#include <string>
using namespace std;

void heapify(vector<string> &v, int pos, int h, int &cnt){
	int j;
	while(2*pos+1 < h){
		j = 2*pos + 1;
		int strcomp;
		if((j + 1 < h) && (v[j+1] > v[j])){
			j++;
		}
		if(v[j] > v[pos]){
			swap(v[j], v[pos]);
			pos = j;
			cnt++;
		}
		else break;
	}
}

void heapsort(vector<string> &v, int h, int &cnt){
	for (int i = (h / 2) - 1; i >= 0; i--)
    {
        heapify(v, i, h, cnt);
    }
	
    for (int i = h - 1; i > 1; i--)
	{
		swap(v[0], v[i]);
		heapify(v, 0, i, cnt);
	}
}

int main(){
	int n, d, h, cnt = 0;
	char * s = new char[100];
	scanf("%d", &n);
	vector<string> a;
	for(int i = 0; i < n; i++){
		cin >> s;
		a.push_back(s);
	}
	h = a.size();
	heapsort(a, h, cnt);
	for(int i = 0; i < n; i++)
		cout << a[i] <<  endl;
	//printf("%d", cnt);
	return 0;
}
